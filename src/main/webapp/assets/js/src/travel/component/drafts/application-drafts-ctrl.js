essTravel.controller('DraftsCtrl',
                     ['$scope', '$window', 'appProps', 'modals', 'TravelDraftsApi', draftsController]);

function draftsController($scope, $window, appProps, modals, draftsApi) {

    $scope.text = "SOME TEXT";
}